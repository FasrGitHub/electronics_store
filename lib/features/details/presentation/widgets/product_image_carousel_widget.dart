import 'package:flutter/material.dart';

class ProductImageCarouselWidget extends StatelessWidget {
  const ProductImageCarouselWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
